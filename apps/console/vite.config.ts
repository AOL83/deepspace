import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pages serves this project from https://aol83.github.io/deepspace/.
  // Change this if the repository name changes.
  base: '/deepspace/'
});
